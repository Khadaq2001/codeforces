#include <iostream>
#include <vector>

using namespace std;
typedef long long ll;
int t,n,a,b;
int main(){
    cin >> t;
    for (int i=0; i<t; i++){
        scanf("%lld %lld %lld", &n, &a, &b); 
        
    }
}
