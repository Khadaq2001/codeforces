#include <iostream>
#include <vector>

using namespace std;

void solve(){
    int n;
    vector<int> a(n);
    for (int &i: a)
        cin >> i;
    
    for (int i=0; i<n-2; i++){
        int[3] trip =
    }
}
